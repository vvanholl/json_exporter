namespace: "front"
httpworkers: 2

rules:
  mapping:
    - path: ["front","networkAdRequest"]
      labels: ["network"]

common:
  interval: 10
  labels:
    dc: "TMK"

endpoints:
  - url: "http://smart25e/api/metrics"
